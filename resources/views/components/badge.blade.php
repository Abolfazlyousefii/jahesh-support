@props(['type' => 'neutral'])
<span {{ $attributes->class(['inline-flex items-center rounded-full px-2.5 py-1 text-xs font-semibold', 'bg-emerald-50 text-emerald-700' => $type === 'success', 'bg-red-50 text-red-700' => $type === 'danger', 'bg-amber-50 text-amber-700' => $type === 'warning', 'bg-sky-50 text-sky-700' => $type === 'info', 'bg-gray-100 text-gray-700' => $type === 'neutral']) }}>{{ $slot }}</span>
